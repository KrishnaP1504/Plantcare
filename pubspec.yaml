name: plant_care_app1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
    
  google_generative_ai: ^0.4.0
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  camera: ^0.11.3
  http: ^1.6.0
  lucide_icons: ^0.257.0
  intl: ^0.20.2
  permission_handler: ^12.0.1
  image_picker: ^1.2.1
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^6.0.0

flutter_icons:
  android: true
  ios: true
  image_path: assets/icon/PlantCare.png


flutter:
  uses-material-design: true
